<#
.SYNOPSIS
    This PowerShell script ensures that the convenience PIN for Windows 10 is disabled.

.NOTES
    Author          : Marlin Brown
    LinkedIn        : linkedin.com/in/marlin-brown-085349274/
    GitHub          : github.com/marlinbrown
    Date Created    : 2025-16-03
    Last Modified   : 2025-16-03
    Version         : 1.0
    CVEs            : N/A
    Plugin IDs      : N/A
    STIG-ID         : WN10-CC-000370

.TESTED ON
    Date(s) Tested  : 
    Tested By       : 
    Systems Tested  : 
    PowerShell Ver. : 

.USAGE
    Put any usage instructions here.
    Example syntax:
    PS C:\> .\__remediation_template(STIG-ID-WN10-AU-000500).ps1 
#>

# Define the registry key and value
$registryKeyPath = "HKCU:\Software\Microsoft\Windows\CurrentVersion\PushNotifications"
$registryValueName = "ShowInkWorkspaceOnHover"
$desiredValue = 0  # 0 to disable Windows Ink Workspace

# Check if the registry key exists
if (Test-Path $registryKeyPath) {
    Write-Host "Registry key exists. Modifying the value..."
} else {
    Write-Host "Registry key does not exist. Creating the key..."
    New-Item -Path $registryKeyPath -Force
}

# Set the registry value to disable the Convenience Pen (Windows Ink Workspace)
Set-ItemProperty -Path $registryKeyPath -Name $registryValueName -Value $desiredValue

# Verify the change
$currentValue = Get-ItemProperty -Path $registryKeyPath -Name $registryValueName
Write-Host "Registry Value for ShowInkWorkspaceOnHover is now set to: $($currentValue.$registryValueName)"

Write-Host "Convenience Pen (Windows Ink Workspace) has been disabled."
